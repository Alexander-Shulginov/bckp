---
import './AboutIntro.scss';
---

<section class="about-intro" aria-label="Intro section">
    <figure class="about-intro__figure">
        <video src="" class="about-intro__video" poster="img/AboutIntro/about-intro.webp"></video>
        <button type="button" class="about-intro__play">
            <svg width="64" height="75" viewBox="0 0 64 75" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M64 37.5L0.249997 74.3061L0.25 0.693917L64 37.5Z" fill="white"></path>
            </svg>
        </button>
        <figcaption class="about-intro__figcaption">Наш автоматизированный склад в Португалии</figcaption>
    </figure>
</section>

<style lang="scss">
    .about-intro {
        margin-bottom: 70px;
        margin-top: -128px;

        @media (max-width: 768px) {
            margin-bottom: 48px;
            margin-top: -24px;
        }

        &__figure {
            margin: 0;
            position: relative;
            max-width: 1440px;
            margin: 0 auto;
        }

        &__figcaption {
            font-size: 15px;
            letter-spacing: 0.045em;
            color: $color-gray;
            max-width: 1315px;
            margin: 0 auto;
            padding: 0 20px;

            @media (max-width: 768px) {
                font-size: 14px;
            }
        }

        &__video {
            width: 100%;
            height: 100%;
            aspect-ratio: 16 / 9;
            object-fit: cover;
        }

        &__play {
            background-color: transparent;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: 2;
            cursor: pointer;

            transition: transform 0.2s ease-in-out;

            @media (any-hover: hover) {
                &:hover {
                    transform: translate(-50%, -50%) scale(1.1);
                }
            }

            @media (max-width: 768px) {
                svg {
                    width: 44px;
                    height: 59px;
                }
            }
        }
    }
</style>
